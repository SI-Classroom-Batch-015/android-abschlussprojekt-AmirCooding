<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Unique_Store" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:colorPrimary">@color/primary</item>
        <item name="colorSecondary">@color/background</item>
        <item name="colorOnSecondary">@color/background</item>
        <item name="android:icon">@color/primary</item>
        <item name="backgroundColor">@color/white</item>
        <!--       <item name="android:windowBackground">@drawable/main_background</item>-->
        <!--change statusBarColor background -->
        <item name="android:statusBarColor">@color/primaryDark</item>
        <!--change icons from status bar color -->
        <item name="android:windowLightStatusBar">false</item>
    </style>
    <style name="Theme.Unique_Store" parent="Base.Theme.Unique_Store" />
</resources>